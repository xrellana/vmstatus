# API URL for the backend
# When deployed with docker-compose in the root directory, use http://server:3000/api/vps-status
# When deployed separately, update this to point to your actual backend URL
VITE_API_URL=http://server:3000/api/vps-status
