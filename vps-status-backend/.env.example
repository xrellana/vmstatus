# Backend Environment Variables Example

# Option 1: Define the primary frontend domain (simpler for single domain)
# The backend will automatically allow https://<your-domain> and http://localhost:5173 (for dev)
# PRIMARY_FRONTEND_DOMAIN=myapp.yourdomain.com

# Option 2: Explicitly define all allowed origins (more flexible for multiple/complex origins)
# This takes precedence over PRIMARY_FRONTEND_DOMAIN if set.
# Separate multiple origins with commas. Include protocol (http/https) and port if non-standard.
# CORS_ALLOWED_ORIGINS=https://myapp.yourdomain.com,http://localhost:5173,https://admin.anotherdomain.com

# Node environment (set to 'production' in production deployments)
# This affects whether http://localhost:5173 is automatically added when using PRIMARY_FRONTEND_DOMAIN
# NODE_ENV=production
